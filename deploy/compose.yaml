# Production stack. Started by the `deploy` job on the self-hosted runner,
# which is installed on the homelab host and uses the host Docker daemon.
name: person-service

services:
  postgres:
    image: postgres:13
    restart: unless-stopped
    environment:
      POSTGRES_DB: persons
      POSTGRES_USER: program
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is not set}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U program -d persons"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: ..
      dockerfile: Dockerfile
    image: person-service:latest
    restart: unless-stopped
    environment:
      PORT: 8080
      POSTGRES_HOST: postgres
      POSTGRES_DB: persons
      POSTGRES_USER: program
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "8585:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
